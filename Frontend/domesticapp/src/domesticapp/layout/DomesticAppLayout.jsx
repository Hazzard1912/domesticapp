export const DomesticAppLayout = ({ children }) => {
  return (
    <>
      {/* Poner elementos compartidos como navbar, sidebar, footer, etc. */}
      <h1>DomesticAppLayout</h1>
      {children}
    </>
  );
};
